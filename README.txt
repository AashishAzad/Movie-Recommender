First run MovieRecommenderFile.py file inside Complete Code Folder.

It will generate a similarity.pkl file which will auto-save in Complete Code Folder. Copy that file and paste it into MovieRecommender Folder(You can also change the path of similarity.pkl file in app.py folder).

Now it is ready to run.

The data is also given in CompleteCode Folder.